import { getGoodsList } from "@/api/getGoodsList.js";
function fn() {
    let tiemr = null
    return function () {
        timer = setTimeout(() => {

        }, 500);
    }
}
const pull = {
    update(el, binding, vnode) {

    }
}
export default pull